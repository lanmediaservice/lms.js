/**
 * @requires Prototype Library
 */

if (Object.isUndefined(LMS)) {
    /** 
     * @namespace Base namespace for misc classes
     */
    var LMS = {};
}

/**
 * @namespace Base namespace for non-visual components
 */
LMS.Components = {};

/**
 * @class LMS.Component
 */
LMS.Component = Class.create({
    /**
     * @name initialize
     * @function
     * @private
     * @memberOf LMS.Component
     * @return void
     */
    initialize: function() {
    }
});